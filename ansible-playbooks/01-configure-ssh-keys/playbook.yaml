# Playbook #1 
- name: Setup SSH Keys
  hosts: all
  tasks:
    - name: Add SSH Key
      delegate_to: localhost
      ansible.builtin.shell: |
        ssh-copy-id -i ~/.ssh/id_rsa.pub alecgriffin@{{inventory_hostname}}

